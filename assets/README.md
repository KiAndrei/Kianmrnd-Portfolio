# Assets

Place your files here:

- `profile.jpg` — your photo (used in the hero section)
- `alex-morgan-resume.pdf` — your résumé (linked in the About section)

Project images are currently CSS gradients. Replace them by adding a `background-image: url(...)` 
to `.project__img--1` through `.project__img--4` in style.css, or swap the divs for `<img>` tags.
